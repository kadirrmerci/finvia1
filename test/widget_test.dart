import 'package:finvia/main.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Finvia app can be constructed', () {
    expect(const FinviaApp(), isA<FinviaApp>());
  });
}
